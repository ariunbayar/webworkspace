# Copy to .env and tweak as needed:  cp .env.example .env
# Host port to expose the PHP workspace on
PHP_PORT=8081
# Directory (inside the container) whose source files are seeded as boxes.
# Defaults to the whole project; narrow it e.g. to /app/web/assets if preferred.
PHP_DEMO_DIR=/app
